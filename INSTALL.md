# 安装教程

## 宝塔面板安装

1. 新建 PHP 站点，PHP 版本选择 8.0 或更高。
2. 新建 MySQL 数据库，记录数据库名、账号和密码。
3. 上传本仓库所有文件到站点根目录。
4. 访问 `https://你的域名/install.php`。
5. 填写数据库信息、后台账号、后台密码和安全码。
6. 登录后台，添加商品并导入卡密。
7. 确认功能正常后，删除或限制访问 `install.php`。

## Nginx 建议

```nginx
location ~ ^/(config\.php|\.env|storage/) {
    deny all;
}
```

## 升级建议

- 升级前备份数据库和 `config.php`。
- 不要覆盖生产环境中的 `config.php`。
- 不要把支付密钥、二维码、订单数据提交到公开仓库。
